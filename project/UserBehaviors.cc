#include "UserBehaviors.h"

/* Ideally UserBehaviors.h would be merged into this file, so there's no need for the separate header.
 *
 *  Then again, there's no need for UserBehaviors.* at all once everyone switches
 *  to using the REGISTER_BEHAVIOR macro in their behaviors' own .cc files... */
